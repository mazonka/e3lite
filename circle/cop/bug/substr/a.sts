#/bin/sh

# preprocessor expansion must be taken in ()

c=$1

out() {
echo ""
echo "=== start $1"
cat $1
echo "=== end $1"
rm -f $1
}

run(){
echo ""
echo "[$1 $2]"
$c $1 $2
if [ "$3" != "" ]; then
test -f $3 && out $3
fi
}

run a1.cr
run -cc a2.cr a2.cr.out
run -cc a3.cr a3.cr.out


