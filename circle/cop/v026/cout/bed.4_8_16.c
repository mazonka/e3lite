// this is a reference file with logic (not bitwise ops)

typedef char bool;

void bed_4_8_16_cr (
    bool x0, bool x1, bool x2, bool x3, bool x4, bool x5, bool x6, bool x7, bool x8, bool x9, bool x10, bool x11, bool x12, bool x13, bool x14, bool x15,
    bool * y0, bool * y1, bool * y2, bool * y3, bool * y4, bool * y5, bool * y6, bool * y7
)
{

    bool kv = 1;
    bool fo = x6 && kv || !x6 && !kv;
    bool ht = x4 && fo || !x4 && !fo;
    bool iy = x7 && ht || !x7 && !ht;
    bool b = x0 && ht || !x0 && iy;
    bool a = x0 && iy || !x0 && ht;
    bool e = x5 && b || !x5 && a;
    bool c = x0 && iy || !x0 && !ht;
    bool d = x0 && ht || !x0 && !iy;
    bool f = x5 && c || !x5 && !d;
    bool gt = x1 && e || !x1 && f;
    bool gs = x1 && f || !x1 && e;
    bool m = x2 && gt || !x2 && !gs;
    bool n = x2 && gs || !x2 && !gt;
    bool g = x15 && m || !x15 && n;
    bool l = x1 && ht || !x1 && !ht;
    bool k = x2 && iy || !x2 && !iy;
    bool h = x15 && l || !x15 && !k;
    bool i = x9 && g || !x9 && h;
    bool j = x9 && h || !x9 && g;
    bool s = x10 && i || !x10 && !j;
    bool p = x15 && k || !x15 && !l;
    bool o = x15 && n || !x15 && m;
    bool r = x9 && p || !x9 && !o;
    bool q = x9 && o || !x9 && !p;
    bool t = x10 && r || !x10 && q;
    bool u = x8 && s || !x8 && t;
    bool v = x8 && t || !x8 && s;
    bool w = x13 && u || !x13 && v;
    bool x = x14 && w || !x14 && !w;
    bool y = x12 && x || !x12 && !x;
    *y0 = !y;
    bool cz = x4 && kv || !x4 && !kv;
    bool dl = x7 && fo || !x7 && !cz;
    bool aa = x0 && dl || !x0 && !fo;
    bool dk = x7 && fo || !x7 && cz;
    bool z = x0 && fo || !x0 && !dk;
    bool ad = x5 && aa || !x5 && z;
    bool dh = x7 && cz || !x7 && fo;
    bool ab = x0 && fo || !x0 && !dh;
    bool dg = x7 && cz || !x7 && !fo;
    bool ac = x0 && dg || !x0 && fo;
    bool ae = x5 && ab || !x5 && !ac;
    bool bc = x1 && ad || !x1 && ae;
    bool af = x0 && fo || !x0 && dg;
    bool ag = x0 && dh || !x0 && !fo;
    bool ak = x5 && af || !x5 && ag;
    bool ai = x0 && dk || !x0 && !fo;
    bool ah = x0 && fo || !x0 && !dl;
    bool aj = x5 && ai || !x5 && ah;
    bool bb = x1 && ak || !x1 && aj;
    bool bi = x2 && bc || !x2 && !bb;
    bool am = x0 && cz || !x0 && dk;
    bool al = x0 && dl || !x0 && !cz;
    bool aq = x5 && am || !x5 && !al;
    bool an = x0 && dg || !x0 && cz;
    bool ao = x0 && cz || !x0 && dh;
    bool ap = x5 && an || !x5 && ao;
    bool az = x1 && aq || !x1 && ap;
    bool as = x0 && dh || !x0 && cz;
    bool ar = x0 && cz || !x0 && dg;
    bool av = x5 && as || !x5 && ar;
    bool au = x0 && cz || !x0 && !dl;
    bool at = x0 && dk || !x0 && cz;
    bool aw = x5 && au || !x5 && at;
    bool ba = x1 && av || !x1 && aw;
    bool bj = x2 && az || !x2 && ba;
    bool bw = x15 && bi || !x15 && !bj;
    bool ds = x0 && dl || !x0 && !dk;
    bool dt = x0 && dg || !x0 && dh;
    bool ay = x1 && ds || !x1 && !dt;
    bool dq = x0 && dh || !x0 && dg;
    bool dr = x0 && dk || !x0 && !dl;
    bool ax = x1 && dq || !x1 && dr;
    bool bk = x2 && ay || !x2 && !ax;
    bool fq = x0 && fo || !x0 && !fo;
    bool bl = x2 && fq || !x2 && !fq;
    bool bx = x15 && bk || !x15 && bl;
    bool bd = x9 && bw || !x9 && bx;
    bool bf = x2 && ax || !x2 && !ay;
    bool bu = x15 && bf || !x15 && cz;
    bool bg = x2 && ba || !x2 && az;
    bool bh = x2 && bb || !x2 && !bc;
    bool bv = x15 && bg || !x15 && bh;
    bool be = x9 && bu || !x9 && bv;
    bool cf = x10 && bd || !x10 && !be;
    bool br = x15 && cz || !x15 && bf;
    bool bq = x15 && bh || !x15 && bg;
    bool bn = x9 && br || !x9 && bq;
    bool bo = x15 && bj || !x15 && !bi;
    bool bp = x15 && bl || !x15 && bk;
    bool bm = x9 && bo || !x9 && !bp;
    bool ce = x10 && bn || !x10 && bm;
    bool cb = x8 && cf || !x8 && !ce;
    bool bs = x9 && bp || !x9 && !bo;
    bool bt = x9 && bq || !x9 && br;
    bool cd = x10 && bs || !x10 && !bt;
    bool by = x9 && bv || !x9 && bu;
    bool bz = x9 && bx || !x9 && bw;
    bool cc = x10 && by || !x10 && !bz;
    bool ca = x8 && cd || !x8 && !cc;
    bool ci = x13 && cb || !x13 && ca;
    bool cg = x8 && cc || !x8 && !cd;
    bool ch = x8 && ce || !x8 && !cf;
    bool cj = x13 && cg || !x13 && ch;
    bool ck = x14 && ci || !x14 && !cj;
    bool cl = x14 && cj || !x14 && !ci;
    bool cm = x12 && ck || !x12 && cl;
    *y1 = cm;
    bool du = x7 && fo || !x7 && !fo;
    bool co = x0 && dk || !x0 && !du;
    bool cn = x0 && du || !x0 && !dl;
    bool cs = x5 && co || !x5 && cn;
    bool cp = x0 && du || !x0 && !dg;
    bool cq = x0 && dh || !x0 && du;
    bool cr = x5 && cp || !x5 && !cq;
    bool eb = x1 && cs || !x1 && !cr;
    bool cu = x0 && du || !x0 && dh;
    bool ct = x0 && dg || !x0 && !du;
    bool cx = x5 && cu || !x5 && ct;
    bool cv = x0 && dl || !x0 && !du;
    bool cw = x0 && du || !x0 && !dk;
    bool cy = x5 && cv || !x5 && cw;
    bool ec = x1 && cx || !x1 && !cy;
    bool ek = x2 && eb || !x2 && !ec;
    bool gg = x7 && cz || !x7 && !cz;
    bool da = x0 && gg || !x0 && dl;
    bool db = x0 && dk || !x0 && !gg;
    bool df = x5 && da || !x5 && !db;
    bool dc = x0 && dh || !x0 && gg;
    bool dd = x0 && gg || !x0 && dg;
    bool de = x5 && dc || !x5 && dd;
    bool dz = x1 && df || !x1 && !de;
    bool dj = x0 && dg || !x0 && gg;
    bool di = x0 && gg || !x0 && dh;
    bool do_ = x5 && dj || !x5 && di;
    bool dm = x0 && gg || !x0 && !dk;
    bool dn = x0 && dl || !x0 && gg;
    bool dp = x5 && dm || !x5 && dn;
    bool ea = x1 && do_ || !x1 && !dp;
    bool ej = x2 && dz || !x2 && ea;
    bool ex = x15 && ek || !x15 && ej;
    bool dy = x1 && dr || !x1 && dq;
    bool dx = x1 && dt || !x1 && !ds;
    bool em = x2 && dy || !x2 && !dx;
    bool dv = x0 && du || !x0 && !du;
    bool dw = x1 && dv || !x1 && !dv;
    bool el = x2 && dw || !x2 && !dw;
    bool ey = x15 && em || !x15 && !el;
    bool ed = x9 && ex || !x9 && !ey;
    bool ef = x2 && dx || !x2 && !dy;
    bool eg = x1 && gg || !x1 && !gg;
    bool ew = x15 && ef || !x15 && !eg;
    bool eh = x2 && ea || !x2 && dz;
    bool ei = x2 && ec || !x2 && !eb;
    bool ev = x15 && eh || !x15 && !ei;
    bool ee = x9 && ew || !x9 && !ev;
    bool ff = x10 && ed || !x10 && !ee;
    bool es = x15 && eg || !x15 && !ef;
    bool er = x15 && ei || !x15 && !eh;
    bool eo = x9 && es || !x9 && !er;
    bool eq = x15 && ej || !x15 && ek;
    bool ep = x15 && el || !x15 && !em;
    bool en = x9 && eq || !x9 && ep;
    bool fg = x10 && eo || !x10 && en;
    bool fc = x8 && ff || !x8 && !fg;
    bool et = x9 && ep || !x9 && eq;
    bool eu = x9 && er || !x9 && !es;
    bool fe = x10 && et || !x10 && !eu;
    bool fa = x9 && ev || !x9 && !ew;
    bool ez = x9 && ey || !x9 && !ex;
    bool fd = x10 && fa || !x10 && !ez;
    bool fb = x8 && fe || !x8 && !fd;
    bool fk = x13 && fc || !x13 && fb;
    bool fi = x8 && fd || !x8 && !fe;
    bool fh = x8 && fg || !x8 && !ff;
    bool fj = x13 && fi || !x13 && fh;
    bool fm = x14 && fk || !x14 && !fj;
    bool fl = x14 && fj || !x14 && !fk;
    bool fn = x12 && fm || !x12 && fl;
    *y2 = !fn;
    bool fp = x5 && fo || !x5 && !fo;
    bool fr = x2 && fp || !x2 && !fp;
    bool fs = x1 && fq || !x1 && !fq;
    bool ft = x9 && fr || !x9 && fs;
    bool fu = x9 && fs || !x9 && fr;
    bool fv = x10 && ft || !x10 && !fu;
    bool fw = x10 && fu || !x10 && !ft;
    bool fx = x8 && fv || !x8 && fw;
    bool fy = x8 && fw || !x8 && fv;
    bool fz = x13 && fx || !x13 && !fy;
    bool ga = x11 && fz || !x11 && !fz;
    bool gb = x3 && ga || !x3 && !ga;
    bool gc = x14 && gb || !x14 && !gb;
    *y3 = gc;
    bool gd = x0 && gg || !x0 && !gg;
    bool ge = x5 && gd || !x5 && !gd;
    bool gf = x1 && ge || !x1 && !ge;
    bool gj = x15 && gf || !x15 && !gf;
    bool gh = x2 && gg || !x2 && !gg;
    bool gi = x15 && gh || !x15 && !gh;
    bool gl = x9 && gj || !x9 && !gi;
    bool gk = x9 && gi || !x9 && !gj;
    bool gm = x10 && gl || !x10 && gk;
    bool gn = x10 && gk || !x10 && gl;
    bool go = x8 && gm || !x8 && !gn;
    bool gp = x8 && gn || !x8 && !gm;
    bool gq = x13 && go || !x13 && !gp;
    bool gr = x12 && gq || !x12 && !gq;
    *y4 = gr;
    bool he = x2 && gs || !x2 && gt;
    bool hf = x2 && gt || !x2 && gs;
    bool gy = x15 && he || !x15 && !hf;
    bool gu = x0 && iy || !x0 && !iy;
    bool gv = x5 && gu || !x5 && !gu;
    bool gw = x1 && gv || !x1 && !gv;
    bool hd = x2 && gw || !x2 && !gw;
    bool gx = x0 && ht || !x0 && !ht;
    bool hc = x5 && gx || !x5 && !gx;
    bool gz = x15 && hd || !x15 && hc;
    bool ha = x9 && gy || !x9 && gz;
    bool hb = x9 && gz || !x9 && gy;
    bool hl = x10 && ha || !x10 && hb;
    bool hh = x15 && hc || !x15 && hd;
    bool hg = x15 && hf || !x15 && !he;
    bool hi = x9 && hh || !x9 && !hg;
    bool hj = x9 && hg || !x9 && !hh;
    bool hk = x10 && hi || !x10 && !hj;
    bool hn = x8 && hl || !x8 && !hk;
    bool hm = x8 && hk || !x8 && !hl;
    bool ho = x13 && hn || !x13 && !hm;
    bool hp = x14 && ho || !x14 && !ho;
    bool hq = x12 && hp || !x12 && !hp;
    *y5 = !hq;
    bool iv = x7 && ht || !x7 && kv;
    bool hr = x0 && iv || !x0 && !iy;
    bool iu = x7 && ht || !x7 && !kv;
    bool hs = x0 && iy || !x0 && !iu;
    bool hx = x5 && hr || !x5 && !hs;
    bool is = x7 && kv || !x7 && !ht;
    bool hv = x0 && iy || !x0 && !is;
    bool it = x7 && kv || !x7 && ht;
    bool hu = x0 && it || !x0 && iy;
    bool hw = x5 && hv || !x5 && hu;
    bool jg = x1 && hx || !x1 && hw;
    bool hz = x0 && iy || !x0 && it;
    bool hy = x0 && is || !x0 && !iy;
    bool id = x5 && hz || !x5 && !hy;
    bool ia = x0 && iu || !x0 && !iy;
    bool ib = x0 && iy || !x0 && !iv;
    bool ic = x5 && ia || !x5 && !ib;
    bool jh = x1 && id || !x1 && ic;
    bool jp = x2 && jg || !x2 && !jh;
    bool jb = x7 && kv || !x7 && !kv;
    bool if_ = x0 && jb || !x0 && iu;
    bool ie = x0 && iv || !x0 && !jb;
    bool ii = x5 && if_ || !x5 && ie;
    bool ih = x0 && it || !x0 && jb;
    bool ig = x0 && jb || !x0 && is;
    bool ij = x5 && ih || !x5 && !ig;
    bool je = x1 && ii || !x1 && ij;
    bool il = x0 && is || !x0 && jb;
    bool ik = x0 && jb || !x0 && it;
    bool io = x5 && il || !x5 && !ik;
    bool im = x0 && jb || !x0 && !iv;
    bool in = x0 && iu || !x0 && jb;
    bool ip = x5 && im || !x5 && !in;
    bool jf = x1 && io || !x1 && ip;
    bool jo = x2 && je || !x2 && jf;
    bool kc = x15 && jp || !x15 && jo;
    bool iq = x0 && iv || !x0 && iu;
    bool ir = x0 && it || !x0 && !is;
    bool iz = x1 && iq || !x1 && ir;
    bool ix = x0 && is || !x0 && !it;
    bool iw = x0 && iu || !x0 && iv;
    bool ja = x1 && ix || !x1 && !iw;
    bool jr = x2 && iz || !x2 && ja;
    bool jq = x1 && iy || !x1 && !iy;
    bool kd = x15 && jr || !x15 && !jq;
    bool ji = x9 && kc || !x9 && kd;
    bool jl = x2 && ja || !x2 && iz;
    bool jc = x0 && jb || !x0 && !jb;
    bool jd = x1 && jc || !x1 && !jc;
    bool jk = x2 && jd || !x2 && !jd;
    bool ka = x15 && jl || !x15 && !jk;
    bool jm = x2 && jf || !x2 && je;
    bool jn = x2 && jh || !x2 && !jg;
    bool kb = x15 && jm || !x15 && !jn;
    bool jj = x9 && ka || !x9 && kb;
    bool kk = x10 && ji || !x10 && !jj;
    bool jx = x15 && jk || !x15 && !jl;
    bool jw = x15 && jn || !x15 && !jm;
    bool js = x9 && jx || !x9 && jw;
    bool jv = x15 && jo || !x15 && jp;
    bool ju = x15 && jq || !x15 && !jr;
    bool jt = x9 && jv || !x9 && !ju;
    bool kl = x10 && js || !x10 && jt;
    bool kh = x8 && kk || !x8 && !kl;
    bool jz = x9 && ju || !x9 && !jv;
    bool jy = x9 && jw || !x9 && jx;
    bool kj = x10 && jz || !x10 && !jy;
    bool ke = x9 && kb || !x9 && ka;
    bool kf = x9 && kd || !x9 && kc;
    bool ki = x10 && ke || !x10 && !kf;
    bool kg = x8 && kj || !x8 && !ki;
    bool ko = x13 && kh || !x13 && !kg;
    bool km = x8 && ki || !x8 && !kj;
    bool kn = x8 && kl || !x8 && !kk;
    bool kp = x13 && km || !x13 && !kn;
    bool kq = x14 && ko || !x14 && !kp;
    bool kr = x14 && kp || !x14 && !ko;
    bool ks = x12 && kq || !x12 && !kr;
    *y6 = !ks;
    bool kt = x0 && kv || !x0 && !kv;
    bool ku = x5 && kt || !x5 && !kt;
    bool kx = x1 && ku || !x1 && !ku;
    bool kw = x2 && kv || !x2 && !kv;
    bool kz = x9 && kx || !x9 && !kw;
    bool ky = x9 && kw || !x9 && !kx;
    bool lb = x10 && kz || !x10 && ky;
    bool la = x10 && ky || !x10 && kz;
    bool lc = x8 && lb || !x8 && !la;
    bool ld = x8 && la || !x8 && !lb;
    bool le = x13 && lc || !x13 && !ld;
    *y7 = le;

}

int main()
{
    bool x0, x1, x2, x3, x4, x5, x6, x7, x8, x9, x10, x11, x12, x13, x14, x15;
    bool y0, y1, y2, y3, y4, y5, y6, y7;
    int i, j;
    for ( i = 0; i < 10000; i++ )
        for ( j = 0; j < 10000; j++ )
        {
            bed_4_8_16_cr (
                x0, x1, x2, x3, x4, x5, x6, x7, x8, x9, x10, x11, x12, x13, x14, x15,
                &y0, &y1, &y2, &y3, &y4, &y5, &y6, &y7);
            x0 = y5;
        }
}


